async function getReporting(){if(!window.DirectCreditData){await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='admin-data.js?v=20260927.1';s.onload=resolve;s.onerror=reject;document.head.appendChild(s);});}return window.DirectCreditData.reporting();}
(async function(){const e=document.getElementById('repayment');if(!e)return;const money=v=>'₹'+Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2});const pct=(a,b)=>b?((Number(a||0)/Number(b))*100).toFixed(1)+'%':'0.0%';try{const d=await getReporting(),s=d.repayment_status||{};const keys=['On-Time / Paid','Overdue DPD 1–30','Overdue DPD 31–60','Overdue DPD 61–90','NPA DPD 90+','Upcoming'];e.innerHTML=`<div class="rm-head"><div><h2><span>5</span> REPAYMENT MATRIX</h2><p>Live-first repayment performance by DPD status</p></div><button class="rm-refresh" onclick="location.reload()">⟳ &nbsp; Refresh</button></div><div class="rm-table-wrap"><table class="rm-table"><thead><tr><th>STATUS</th><th>COUNT</th><th>DUE</th><th>PAID</th><th>UNPAID</th><th>% PAID</th></tr></thead><tbody>${keys.map(k=>{const x=s[k]||{};return `<tr><td>${k}</td><td>${x.count||0}</td><td>${money(x.due)}</td><td>${money(x.paid)}</td><td>${money(x.unpaid)}</td><td>${pct(x.paid,x.due)}</td></tr>`}).join('')}</tbody></table></div>`; }catch(err){e.innerHTML='<div class="rm-table-wrap"><h3>Reporting data unavailable</h3><p>Unable to load the DirectCredit reporting service.</p></div>';}})();
function renderAccounting(){const el=document.getElementById('accounting');if(!el)return;el.innerHTML='<div class="accounting-page"><div class="accounting-head"><div><h2>Loan Accounting & Repayment</h2><p>Live ledger and repayment position from the DirectCredit database.</p></div><button class="accounting-btn" id="accountingRefresh">Refresh</button></div><div id="accountingLive"><div class="accounting-panel"><h3>Loading live accounting data…</h3></div></div></div>';document.getElementById('accountingRefresh').onclick=loadAccounting;loadAccounting()}
function money(v){return '₹'+Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2})}
async function loadAccounting(){const root=document.getElementById('accountingLive');if(!root)return;root.innerHTML='<div class="accounting-panel"><h3>Loading live accounting data…</h3></div>';try{const d=await DirectCreditData.reporting();const m=d.monthly||[];const active=d.active_loans||0;const outstanding=d.amounts?.outstanding||0;const overdue=d.amounts?.overdue||0;const paid=d.amounts?.paid||0;const disbursed=d.amounts?.disbursed||0;const unpaid=d.amounts?.unpaid||0;const bars=m.slice(-6);const max=Math.max(...bars.map(x=>Number(x.disbursed_amount||0)),1);root.innerHTML=`<div class="accounting-kpis"><div class="accounting-kpi navy"><div class="kpi-title">Active Loans</div><div class="kpi-value">${active}</div><div class="kpi-sub">${d.disbursed_count||0} disbursed</div></div><div class="accounting-kpi green"><div class="kpi-title">Outstanding</div><div class="kpi-value">${money(outstanding)}</div><div class="kpi-sub">Current portfolio</div></div><div class="accounting-kpi blue"><div class="kpi-title">Amount Received</div><div class="kpi-value">${money(paid)}</div><div class="kpi-sub">Repayment ledger</div></div><div class="accounting-kpi orange"><div class="kpi-title">Overdue</div><div class="kpi-value">${money(overdue)}</div><div class="kpi-sub">Unpaid overdue amount</div></div><div class="accounting-kpi purple"><div class="kpi-title">Unpaid</div><div class="kpi-value">${money(unpaid)}</div><div class="kpi-sub">Across repayment schedule</div></div></div><div class="accounting-grid"><div class="accounting-panel"><h3>DISBURSEMENT TREND</h3><div class="accounting-chart live-bars">${bars.length?bars.map(x=>`<div class="chart-col"><div class="chart-bar" style="height:${Math.max(5,(Number(x.disbursed_amount||0)/max)*100)}%"><span>${money(x.disbursed_amount)}</span></div><span class="chart-label">${x.month}</span></div>`).join(''):'<div class="live-empty">No live disbursement data available.</div>'}</div><div class="chart-legend">${bars.length?'Database-backed monthly disbursement':'No data'}</div></div><div class="accounting-panel"><h3>PORTFOLIO POSITION</h3><div class="repay-summary"><div class="repay-row"><span>Total due</span><strong>${money(d.amounts?.due)}</strong></div><div class="repay-row"><span>Received</span><strong>${money(paid)}</strong></div><div class="repay-row"><span>Outstanding</span><strong>${money(outstanding)}</strong></div><div class="repay-row"><span>Overdue</span><strong class="repay-eff">${money(overdue)}</strong></div></div></div></div><div class="accounting-bottom"><div class="accounting-panel"><h3>RECENT REPAYMENTS</h3><div class="table-scroll"><table class="accounting-table"><thead><tr><th>LOAN ID</th><th>CUSTOMER ID</th><th>LOAN AMOUNT</th><th>RECEIVED</th><th>DATE</th><th>STATUS</th></tr></thead><tbody>${(d.recent_repayments||[]).length?(d.recent_repayments||[]).map(r=>`<tr><td>${r.loan_id}</td><td>${r.customer_id??'—'}</td><td>${money(r.loan_amount)}</td><td class="green-text">${money(r.paid_amount)}</td><td>${r.date||'—'}</td><td>${r.status||'—'}</td></tr>`).join(''):'<tr><td colspan="6">No live repayment records available.</td></tr>'}</tbody></table></div></div><div class="accounting-panel"><h3>ACCOUNTING COVERAGE</h3><div class="repay-summary"><div class="repay-row"><span>Applications</span><strong>${d.applications||0}</strong></div><div class="repay-row"><span>Repayment records</span><strong>${d.repayments||0}</strong></div><div class="repay-row"><span>Overdue loans</span><strong>${d.overdue_loans||0}</strong></div><div class="repay-row"><span>Repaid loans</span><strong>${d.repaid_loans||0}</strong></div></div></div></div>`}catch(e){root.innerHTML='<div class="accounting-panel"><h3>Live data unavailable</h3><p>Connect the DirectCredit API. No demo accounting values are shown.</p></div>'}}
document.addEventListener('DOMContentLoaded',()=>{if(window.DirectCreditData)renderAccounting();else{const s=document.createElement('script');s.src='admin-data.js';s.onload=renderAccounting;document.head.appendChild(s)}});
document.addEventListener('DOMContentLoaded',async()=>{const id=new URLSearchParams(location.search).get('customer_id');const host=document.getElementById('live');const base=(window.DIRECTCREDIT_API_URL||localStorage.getItem('directcredit_api_url')||'/api').replace(/\/$/,'');if(!id||!/^[0-9]+$/.test(id)){host.innerHTML='<h2>No customer selected</h2><p>Open this page with a valid customer_id from a live customer record.</p>';return;}try{const r=await fetch(`${base}/customers/${id}/profile`,{headers:{Accept:'application/json'}});if(!r.ok)throw Error(r.status);const d=await r.json(),c=d.customer||{},risk=d.risk_score||{};const esc=v=>String(v??'Not available').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));const official=risk.source==='scorecard'||d.directcredit_score!=null;host.innerHTML=`<h2>${esc(c.name||'Customer')}</h2><p>Customer ID: ${esc(c.customer_code||c.id)}</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px;margin-top:18px"><div class="card"><small>DIRECTCREDIT SCORE</small><h2>${official?esc(d.directcredit_score??risk.total_score):'Not assessed'}</h2></div><div class="card"><small>RISK TIER</small><h3>${esc(risk.risk_tier||'Not assessed')}</h3></div><div class="card"><small>DECISION</small><h3>${esc(risk.decision||'Not assessed')}</h3></div><div class="card"><small>CIBIL / BUREAU SCORE</small><h3>${c.cibil_score==null||c.cibil_score===0?'Not available':esc(c.cibil_score)}</h3></div><div class="card"><small>FOIR</small><h3>${c.foir==null||c.foir===0?'Not available':esc(c.foir+'%')}</h3></div></div><p style="margin-top:18px">${official?'Score shown from the official scorecard result recorded in the system.':'No official DirectCredit scorecard result is recorded. No derived or dummy score is displayed.'}</p>`;}catch(e){host.innerHTML='<h2>Live risk data unavailable</h2><p>No demo risk score or approval decision is displayed.</p>';}});
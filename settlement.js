function renderSettlement(){const el=document.getElementById('settlement');if(!el)return;el.innerHTML='<div class="settlement-page"><div class="settlement-head"><div><h2>Loan Settlement & Closure</h2><p>Live closure view calculated from current loan records.</p></div><button class="settlement-btn" id="settlementRefresh">Refresh</button></div><div id="settlementLive"><div class="settlement-panel"><h3>Loading live settlement data…</h3></div></div></div>';document.getElementById('settlementRefresh').onclick=loadSettlement;loadSettlement()}
function money(v){return '₹'+Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2})}
async function loadSettlement(){const root=document.getElementById('settlementLive');if(!root)return;root.innerHTML='<div class="settlement-panel"><h3>Loading live settlement data…</h3></div>';try{const d=await DirectCreditData.reporting();const closed=(d.recent_loans||[]).filter(x=>x.status==='repaid');const allClosedCount=d.repaid_loans||0;const allClosedAmount=closed.reduce((s,x)=>s+Number(x.amount||0),0);const monthly=(d.monthly||[]).slice(-6);root.innerHTML=`<div class="settlement-kpis"><div class="settlement-kpi purple"><div class="kpi-title">Loans Repaid</div><div class="kpi-value">${allClosedCount}</div><div class="kpi-sub">Live database count</div></div><div class="settlement-kpi blue"><div class="kpi-title">Repaid Amount</div><div class="kpi-value">${money(allClosedAmount)}</div><div class="kpi-sub">From currently returned loan records</div></div><div class="settlement-kpi green"><div class="kpi-title">Active Loans</div><div class="kpi-value">${d.active_loans||0}</div><div class="kpi-sub">Still open</div></div><div class="settlement-kpi orange"><div class="kpi-title">Overdue Loans</div><div class="kpi-value">${d.overdue_loans||0}</div><div class="kpi-sub">Needs collection action</div></div><div class="settlement-kpi red"><div class="kpi-title">Outstanding</div><div class="kpi-value">${money(d.amounts?.outstanding)}</div><div class="kpi-sub">Current portfolio</div></div><div class="settlement-kpi cyan"><div class="kpi-title">Data Updated</div><div class="kpi-value">Live</div><div class="kpi-sub">${d.generated_at?new Date(d.generated_at).toLocaleString('en-IN'):'—'}</div></div></div><div class="settlement-grid"><div class="settlement-panel"><h3>DISBURSEMENT / CLOSURE ACTIVITY</h3><div class="chart-area live-settlement-bars">${monthly.length?monthly.map(x=>`<div class="bar-col"><div class="bar" style="height:${Math.max(5,Number(x.disbursed_count||0)/Math.max(...monthly.map(y=>Number(y.disbursed_count||0)),1)*100)}%"><span>${x.disbursed_count||0}</span></div><div class="bar-label">${x.month}</div></div>`).join(''):'<div class="live-empty">No live activity available.</div>'}</div></div><div class="settlement-panel"><h3>RECOVERY POSITION</h3><div class="summary-row"><span>Total due</span><strong>${money(d.amounts?.due)}</strong></div><div class="summary-row"><span>Total received</span><strong class="eff">${money(d.amounts?.paid)}</strong></div><div class="summary-row"><span>Unpaid</span><strong>${money(d.amounts?.unpaid)}</strong></div><div class="summary-row"><span>Overdue</span><strong>${money(d.amounts?.overdue)}</strong></div></div></div><div class="settlement-bottom"><div class="settlement-panel"><h3>RECENT REPAID LOANS</h3><div class="table-scroll"><table class="settlement-table"><thead><tr><th>LOAN ID</th><th>CUSTOMER</th><th>AMOUNT</th><th>STATUS</th><th>CREATED</th></tr></thead><tbody>${closed.length?closed.map(x=>`<tr><td>${x.id}</td><td>${x.customer_id}</td><td>${money(x.amount)}</td><td><span class="closed">${x.status}</span></td><td>${x.created_at?new Date(x.created_at).toLocaleDateString('en-IN'):'—'}</td></tr>`).join(''):'<tr><td colspan="5">No live repaid loans available.</td></tr>'}</tbody></table></div></div><div class="settlement-panel"><h3>PORTFOLIO STATUS</h3><div class="summary-row"><span>Applications</span><strong>${d.applications||0}</strong></div><div class="summary-row"><span>Disbursed</span><strong>${d.disbursed_count||0}</strong></div><div class="summary-row"><span>Repaid</span><strong class="eff">${d.repaid_loans||0}</strong></div><div class="summary-row"><span>Rejected / Dropped</span><strong>${d.rejected||0}</strong></div></div></div>`}catch(e){root.innerHTML='<div class="settlement-panel"><h3>Live data unavailable</h3><p>Connect the DirectCredit API. No demo settlement values are shown.</p></div>'}}
document.addEventListener('DOMContentLoaded',()=>{if(window.DirectCreditData)renderSettlement();else{const s=document.createElement('script');s.src='admin-data.js';s.onload=renderSettlement;document.head.appendChild(s)}});
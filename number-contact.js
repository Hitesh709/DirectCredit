document.addEventListener('DOMContentLoaded',async()=>{const id=new URLSearchParams(location.search).get('customer_id');const host=document.getElementById('live');const base=(window.DIRECTCREDIT_API_URL||localStorage.getItem('directcredit_api_url')||'/api').replace(/\/$/,'');if(!id||!/^[0-9]+$/.test(id)){host.innerHTML='<h2>No customer selected</h2><p>Open this page with a valid customer_id from a live customer record.</p>';return;}try{const r=await fetch(`${base}/customers/${id}/profile`,{headers:{Accept:'application/json'}});if(!r.ok)throw Error(r.status);const d=await r.json(),c=d.customer||{};const esc=v=>String(v??'Not available').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));const rows=[['Customer ID',c.customer_code||c.id],['Registered Mobile',c.mobile],['Email',c.email],['Current City',c.current_city],['Address',c.address],['Permanent Address',c.permanent_address],['Email Verification',c.email_verified],['Residence Ownership',c.residence_ownership],['Residence Since',c.residence_since]];host.innerHTML=`<h2>${esc(c.name||'Customer')}</h2><p>Only contact fields stored in the DirectCredit database are shown.</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:12px;margin-top:18px">${rows.map(x=>`<div class="panel"><small>${esc(x[0])}</small><h3>${esc(x[1])}</h3></div>`).join('')}</div>`;}catch(e){host.innerHTML='<h2>Live contact data unavailable</h2><p>No demo contact information is displayed.</p>';}});
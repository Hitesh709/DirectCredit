(async function(){
 const el=document.getElementById('pipeline');if(!el)return;const base=(localStorage.getItem('directcredit_api_url')||window.DIRECTCREDIT_API_URL||'/api').replace(/\/$/,'');const pct=(a,b)=>b?((a/b)*100).toFixed(1)+'%':'0.0%';
 try{const r=await fetch(base+'/admin/reporting');if(!r.ok)throw Error(r.status);const d=await r.json();const cards=[['Total Applications',d.applications,'Live','purple'],['Unique Applicants',d.unique_users,'Live','blue'],['Repeat Applicants',d.repeat_users,pct(d.repeat_users,d.unique_users),'green'],['Rejected Loans',d.rejected,pct(d.rejected,d.applications),'red'],['Repaid',d.repaid_loans,pct(d.repaid_loans,d.applications),'green'],['Overdue',d.overdue_loans,pct(d.overdue_loans,d.applications),'red'],['Pending',d.pending,pct(d.pending,d.applications),'blue']];const rows=[['Applications',d.applications,'100%','Tracked'],['Unique Users',d.unique_users,pct(d.unique_users,d.applications),'Tracked'],['Repeat Users',d.repeat_users,pct(d.repeat_users,d.unique_users),'Tracked'],['Pending',d.pending,pct(d.pending,d.applications),'Pending'],['Rejected / Dropped',d.rejected,pct(d.rejected,d.applications),'Rejected'],['Disbursement',d.disbursed_count,pct(d.disbursed_count,d.applications),'Disbursed'],['Active',d.active_loans,pct(d.active_loans,d.applications),'Active'],['Overdue',d.overdue_loans,pct(d.overdue_loans,d.applications),'Overdue'],['Repaid',d.repaid_loans,pct(d.repaid_loans,d.applications),'Completed']];el.innerHTML=`<div class="lp-head"><div><h2><span>2</span> LOAN PIPELINE &amp; FUNNEL</h2><p>Live application to disbursement pipeline</p></div><div class="lp-actions"><button onclick="location.reload()">⟳ &nbsp;Refresh</button></div></div><div class="lp-kpis">${cards.map(c=>`<div class="lp-kpi ${c[3]}"><div class="lp-icon">${c[3]==='red'?'×':c[3]==='green'?'✓':c[3]==='purple'?'▤':'♙'}</div><div><b>${c[0]}</b><strong>${c[1]}</strong><small>${c[2]}</small></div></div>`).join('')}</div><div class="lp-panel"><h3>Loan Application Funnel</h3><div class="lp-table-wrap"><table class="lp-table"><thead><tr><th>STAGE</th><th>COUNT</th><th>% OF PREVIOUS / BASE</th><th>STATUS</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r[0]}</td><td>${r[1]}</td><td>${r[2]}</td><td>${r[3]}</td></tr>`).join('')}</tbody></table></div></div>`;}catch(e){el.innerHTML='<div class="lp-panel"><h3>Live data unavailable</h3><p>Connect the DirectCredit API. No demo pipeline totals are displayed.</p></div>';}
})();

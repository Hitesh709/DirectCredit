const pages=[...document.querySelectorAll('.page')];const navs=[...document.querySelectorAll('.nav')];const titles={dashboard:'Dashboard',application:'Loan Application',users:'Registered Users',slab:'Loan Slab Performance Matrix',repayment:'Repayment Matrix',disbursement:'Disbursement Matrix',calendar:'Due Calendar',accounting:'Loan Accounting & Repayment',profile:'Customer Profile',bank:'Bank Statement Analysis',risk:'Risk & Score Breakdown',trend:'Loan Trend & Summary',kyc:'KYC & Employment Summary',contact:'Number & Contact Details',eligibility:'Loan Request & Eligibility',approval:'Loan Approval Decision'};function showPage(id){pages.forEach(p=>p.classList.toggle('activePage',p.id===id));navs.forEach(n=>n.classList.toggle('active',n.dataset.page===id));document.getElementById('pageTitle').textContent=titles[id]||id;history.replaceState(null,'','#'+id);window.scrollTo({top:0,behavior:'smooth'})}navs.forEach(n=>n.addEventListener('click',()=>showPage(n.dataset.page)));function refreshPage(){location.reload()}function submitApplication(e){e.preventDefault();alert('Loan application submitted successfully. Next step: Credit and Bank Analysis.');showPage('risk')}const initial=location.hash.slice(1);if(initial&&titles[initial])showPage(initial);
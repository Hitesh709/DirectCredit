(async function(){
 const e=document.getElementById('calendar');if(!e)return;const base=(localStorage.getItem('directcredit_api_url')||window.DIRECTCREDIT_API_URL||'/api').replace(/\/$/,'');const money=v=>'₹'+Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2});
 try{const r=await fetch(base+'/admin/reporting');if(!r.ok)throw Error(r.status);const d=await r.json();const rows=d.due_calendar;e.innerHTML=`<div class="dc-cal-head"><h2><span>6.</span> DUE CALENDAR</h2><div class="dc-cal-actions"><button class="dc-cal-btn" onclick="window.print()">⇩ &nbsp; Export</button><button class="dc-cal-btn">☰ &nbsp; Filter</button></div></div><div class="dc-cal-kpis"><div class="dc-cal-kpi red"><div class="label">TOTAL OVERDUE</div><strong>${money(d.amounts.overdue)}</strong><small>${d.overdue_loans} loans</small></div><div class="dc-cal-kpi green"><div class="label">TOTAL DUE</div><strong>${money(d.amounts.due)}</strong><small>${d.repayments} repayment records</small></div><div class="dc-cal-kpi blue"><div class="label">TOTAL PAID</div><strong>${money(d.amounts.paid)}</strong><small>Live ledger</small></div><div class="dc-cal-kpi dark"><div class="label">TOTAL UNPAID</div><strong>${money(d.amounts.unpaid)}</strong><small>Due less paid</small></div></div><div class="dc-cal-panel"><div class="dc-cal-panel-head">Due schedule — Live database</div><div class="dc-cal-table-wrap"><table class="dc-cal-table"><thead><tr><th>DUE DATE</th><th>AMOUNT DUE (₹)</th><th>CASE COUNT</th><th>PAID (₹)</th><th>UNPAID (₹)</th><th>STATUS</th></tr></thead><tbody>${rows.length?rows.map(x=>`<tr><td class="date">${x.date}</td><td class="amount">${money(x.due)}</td><td>${x.count}</td><td class="expected">${money(x.paid)}</td><td>${money(x.due-x.paid)}</td><td><span class="status-overdue">${x.due>x.paid?'Due':'Paid'}</span></td></tr>`).join(''):'<tr><td colspan="6">No repayment due dates available.</td></tr>'}</tbody></table></div></div>`;}catch(err){e.innerHTML='<div class="dc-cal-panel"><h3>Live data unavailable</h3><p>Connect the DirectCredit API. No demo calendar totals are displayed.</p></div>';}
})();

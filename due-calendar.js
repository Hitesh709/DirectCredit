async function getReporting(){if(!window.DirectCreditData){await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='admin-data.js?v=20260927.1';s.onload=resolve;s.onerror=reject;document.head.appendChild(s);});}return window.DirectCreditData.reporting();}
(async function(){const e=document.getElementById('calendar');if(!e)return;const money=v=>'₹'+Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:2});try{const d=await getReporting(),rows=d.due_calendar||[];e.innerHTML=`<div class="dc-cal-head"><h2><span>6.</span> DUE CALENDAR</h2><div class="dc-cal-actions"><button class="dc-cal-btn" onclick="window.print()">⇩ &nbsp; Export</button><button class="dc-cal-btn" onclick="location.reload()">⟳ &nbsp; Refresh</button></div></div><div class="dc-cal-kpis"><div class="dc-cal-kpi red"><div class="label">TOTAL OVERDUE</div><strong>${money(d.amounts?.overdue)}</strong><small>${d.overdue_loans||0} loans</small></div><div class="dc-cal-kpi green"><div class="label">TOTAL DUE</div><strong>${money(d.amounts?.due)}</strong><small>${d.repayments||0} repayment records</small></div><div class="dc-cal-kpi blue"><div class="label">TOTAL PAID</div><strong>${money(d.amounts?.paid)}</strong><small>Reporting ledger</small></div><div class="dc-cal-kpi dark"><div class="label">TOTAL UNPAID</div><strong>${money(d.amounts?.unpaid)}</strong><small>Due less paid</small></div></div><div class="dc-cal-panel"><div class="dc-cal-panel-head">Due schedule — live-first reporting</div><div class="dc-cal-table-wrap"><table class="dc-cal-table"><thead><tr><th>DUE DATE</th><th>AMOUNT DUE (₹)</th><th>CASE COUNT</th><th>PAID (₹)</th><th>UNPAID (₹)</th><th>STATUS</th></tr></thead><tbody>${rows.length?rows.map(x=>`<tr><td class="date">${x.date}</td><td class="amount">${money(x.due)}</td><td>${x.count||0}</td><td class="expected">${money(x.paid)}</td><td>${money(x.unpaid??(Number(x.due||0)-Number(x.paid||0)))}</td><td><span class="status-overdue">${Number(x.unpaid??(Number(x.due||0)-Number(x.paid||0)))>0?'Due':'Paid'}</span></td></tr>`).join(''):'<tr><td colspan="6">No repayment due dates available.</td></tr>'}</tbody></table></div></div>`; }catch(err){e.innerHTML='<div class="dc-cal-panel"><h3>Reporting data unavailable</h3><p>Unable to load the DirectCredit reporting service.</p></div>';}})();
document.addEventListener('DOMContentLoaded',async()=>{const id=new URLSearchParams(location.search).get('customer_id');const host=document.getElementById('live');const base=(window.DIRECTCREDIT_API_URL||localStorage.getItem('directcredit_api_url')||'/api').replace(/\/$/,'');if(!id||!/^[0-9]+$/.test(id)){host.innerHTML='<h2>No customer selected</h2><p>Open this page with a valid customer_id from a live customer record.</p>';return;}try{const r=await fetch(`${base}/customers/${id}/profile`,{headers:{Accept:'application/json'}});if(!r.ok)throw Error(r.status);const d=await r.json(),c=d.customer||{},k=d.kyc_employment||{};const esc=v=>String(v??'Not available').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));const rows=[['Customer ID',c.customer_code||c.id],['KYC Status',k.kyc_status],['Occupation',k.employment_type],['Monthly Income',k.income==null?null:`₹${Number(k.income).toLocaleString('en-IN')}`],['Work Experience',k.work_experience_years==null?null:`${k.work_experience_years} years`],['Years in Business',k.years_in_business==null?null:`${k.years_in_business} years`],['Business Name',c.business_name],['Business Type',c.business_type],['Residence Ownership',k.residence_ownership],['Ownership Proof',k.ownership_proof_status],['PAN',c.pan],['Aadhaar',c.aadhaar_masked]];host.innerHTML=`<h2>${esc(c.name||'Customer')}</h2><p>Only fields stored in DirectCredit are displayed. Verification is not claimed unless recorded by an authorized source.</p><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px;margin-top:18px">${rows.map(x=>`<div class="panel"><small>${esc(x[0])}</small><h3>${esc(x[1])}</h3></div>`).join('')}</div>`;}catch(e){host.innerHTML='<h2>Live KYC data unavailable</h2><p>No demo KYC or employment values are displayed.</p>';}});